import React from "react";

const payroll = () => {
  return (
    <section className="w-full bg-(--primary-blue) py-28">
      <div className="max-w-7xl mx-auto flex justify-center items-center">
        <h1 className="text-[44px]">
          Paystat brings all your payroll operations into one structured system.
          From employee compensation to monthly payroll processing, everything
          is organized, consistent, and easy to manage.
        </h1>
      </div>
    </section>
  );
};

export default payroll;
