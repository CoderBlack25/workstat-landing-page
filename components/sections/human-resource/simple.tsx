const simple = () => {
  return (
    <section className="w-full bg-(--primary-blue) py-28 text-white overflow-hidden">
      <div className="max-w-7xl mx-auto px-30 flex flex-col justify-center items-center text-center gap-6">
        <p className="font-medium">
          Simple for employees. Structured for organizations.
        </p>
        <h1 className="text-[44px]">
          Employees interact with a clean and intuitive interface, while
          administrators maintain full control over access, permissions, and
          data visibility.
        </h1>
      </div>
    </section>
  );
};

export default simple;
